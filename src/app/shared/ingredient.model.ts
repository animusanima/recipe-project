export class Ingredient {
  constructor(public name: string, public amount: number, public indication: string) {
    this.name = name;
    this.amount = amount;
    this.indication = indication;
  }

}
